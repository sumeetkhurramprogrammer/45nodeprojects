//They think of something you could store in a TypeScript Object. Write a program that creates Objects containing these items.

let item1: { name: string, type: string } = { name: "Laptop", type: "Electronic"    } ;
let item2: { name: string, type: string } = { name: "Backpack", type: "Accessory"   } ;
let item3: { name: string, type: string } = { name: "Sunglasses", type: "Accessory" };

// Print the objects
console.log("Item 1:", item1);
console.log("Item 2:", item2);
console.log("Item 3:", item3);